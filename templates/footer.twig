    <div class="container">
      <div class="row">
        <div class="col-md-2">{{ footer_1 }}</div>
        <div class="col-md-3">{{ footer_2 }}</div>
        <div class="col-md-3">{{ footer_3 }}</div>
        <div class="col-md-4 newsletter">{{ footer_4 }}</div>
      </div>
      <div class="bottom">
        <ul>
          <li><a href="#">&copy; Copyright {{"now"|date('Y')}} {{ site.name }}</a></li>
          {% if links %}
            {% for item in links %}
              <li><a href="{{ item.link }}" target="{{ item.target }}">{{ item.title }}</a></li>
            {% endfor %}
          {% endif %}
        </ul>
      </div>
    </div>